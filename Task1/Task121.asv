
function Task121 = Task121(g, x0, TOL)
    i = 0;
    while(true)
        Task121 = f(x0);
        fprintf('x0_{%d}=f(x_{%d})=%.9f\n',i+1,i,Task121);
        if (abs(Task121-x) < TOL; break; end
        i = i+1;
        x0 = Task121;
    end
end